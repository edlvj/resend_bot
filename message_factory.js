var MessageFactory = function (bot, msg) {

	console.log(msg);
	// if(message.text) {
	// 	var answer = new Message().text(message.text).to(message.chat.id);
	// 	bot.send(answer)
	// }

		// if(message.text) {
	// 	var answer = new Message().text(message.text).to(message.chat.id);
	// 	bot.send(answer)
	// }



}

module.exports = MessageFactory
